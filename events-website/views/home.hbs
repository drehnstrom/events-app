<ul>
  {{#events}}
  <hr />
  <div class="container">
    <h2>{{ title }}</h2>
    <div class="h4">{{ description }}</div>
    <div>When: {{ event_time }}</div>
    <div>Where: {{ location }}</div>
    <div><a href="#?{{ id }}">Likes</a> {{ likes }} </div>
    <small class="text-muted">{{ datetime_added }}</small>
  </div>
  {{/events}}
</ul>